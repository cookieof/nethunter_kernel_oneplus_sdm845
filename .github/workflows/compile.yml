name: Build NetHunter Kernel

on:
  workflow_dispatch: # 允许你手动点击按钮触发编译

permissions:
  contents: write

jobs:
  build-kernel:
    name: Build Kernel via GitHub Actions
    runs-on: ubuntu-latest
    steps:
      - name: Fix GCC Conflict Dependencies
        run: |
          sudo apt-get remove -y gcc-multilib g++-multilib || true
          sudo apt-get autoremove -y || true

      - name: Build Kernel
        uses: dabao1955/kernel_build_action@main
        with:
          kernel-url: https://github.com/V0lk3n/nethunter_kernel_oneplus_sdm845
          kernel-branch: nethunter-lineage-22.2
          config: sdm845_defconfig 
          arch: arm64
          
          # 【核心调整 1】关闭古老的 AOSP GCC，全面拥抱全 Clang 编译方案
          aosp-gcc: false
          aosp-clang: true
          
          # 【核心调整 2】直接拉满到真实存在、极为现代的官方 Android 14 编译链
          android-version: 14
          aosp-clang-version: r487747c
          
          anykernel3: true
          release: true
          access-token: ${{ secrets.GITHUB_TOKEN }}
          
          # 【核心调整 3】配合高级编译器，注入完整且绝对兼容的现代 LLVM 变量
          extra-make-args: |
            LLVM=1
            LLVM_IAS=1
            CLANG_TRIPLE=aarch64-linux-gnu-
            LD=ld.lld
            AR=llvm-ar
            NM=llvm-nm
            OBJCOPY=llvm-objcopy
            OBJDUMP=llvm-objdump
            STRIP=llvm-strip
